/**
 * All supported actions for the left button on the GlobalAppHeaderContainer.
 */
export enum GlobalAppHeaderContainerAction{
    AllProjects = "projects",
    NewProject = "new",
    Back = "<BACK>"
}